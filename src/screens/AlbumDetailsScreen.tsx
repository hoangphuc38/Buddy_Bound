import React from 'react';
import { View } from 'react-native';
import { AlbumDetailsScreenProps } from '../types/navigator.type';

const AlbumDetailsScreen = ({ navigation }: AlbumDetailsScreenProps) => {
    return (
        <View />
    );
};

export default AlbumDetailsScreen;
